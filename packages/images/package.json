{
  "private": true,
  "name": "Images",
  "version": "1.0.0",
  "description": "Images of Los Roques",
  "main": "index.js",
  "scripts": {
    "build": "wrangler build",
    "publish": "wrangler publish",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "John Bampton",
  "devDependencies": {
    "prettier": "3.5.3"
  }
}
