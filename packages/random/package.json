{
  "private": true,
  "name": "Random",
  "version": "1.0.0",
  "description": "Random Los Roques",
  "main": "index.js",
  "scripts": {
    "build": "wrangler build",
    "publish": "wrangler publish",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "John Bampton",
  "devDependencies": {
    "prettier": "2.8.8"
  }
}
